"""Week08 retrieval compatibility package.

The lesson deck uses `pipelines/retrieve/*` as the conceptual path. Runtime
retrieval lives in `services/rag_api/app/retrieval.py` so the API and tests share
one implementation. This package provides thin aliases only.
"""

